run = "npm install && cd second-brain-backend && npx prisma db push && npx prisma db seed && cd .. && npx turbo dev"

[nix]
channel = "stable-23_11"

[env]
PORT = "8080"

[deployment]
run = "npm install && npx turbo dev"
deploymentTarget = "cloudrun"
